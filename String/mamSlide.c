/*
    Strings in C => strings are maintained as arrays of characters.
    • Representing strings in C
     stored in arrays of characters
     array can be of any length
     end of string is indicated by a delimiter, the zero/null character '\0'

    String literal
     values are represented by sequences of characters between double quotes ("-")
    • "H" versus 'H'
     'H' is a single character value (stored in 1 byte) as the ASCII value for H (72)
     "H" is an array with two characters, the first is H, the second is the null character value \0
*/